SubDirList(SUBDIRS ${CMAKE_CURRENT_SOURCE_DIR})

if (NOT OPENCV_FOUND)
    list(REMOVE_ITEM SUBDIRS MapGen2D)
endif()

FOREACH(subdir ${SUBDIRS})
  ADD_SUBDIRECTORY(${subdir})
ENDFOREACH()

set(PROJECT_PLUGINS ${PROJECT_PLUGINS} PARENT_SCOPE)
